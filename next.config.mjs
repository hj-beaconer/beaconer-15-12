// /** @type {import('next').NextConfig} */
// const nextConfig = {};
// module.exports = {
//   images: {
//     domains: ["localhost", "cms.beaconer.io"], // Add localhost and any other domain you use for images
//   },
// };

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "cms.beaconer.io"], // Add localhost and any other domain you use for images
  },
};

export default nextConfig;
